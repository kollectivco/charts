name: Production Release Build

on:
  push:
    tags:
      - 'v*'

jobs:
  build_release:
    name: Build and Upload Release Archive
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Preparation
        run: |
          mkdir -p build/kontentainment-lists
          cp -r admin build/kontentainment-lists/
          cp -r inc build/kontentainment-lists/
          cp -r public build/kontentainment-lists/
          cp -r languages build/kontentainment-lists/ 2>/dev/null || :
          cp kontentainment-lists.php build/kontentainment-lists/
          cp charts.php build/kontentainment-lists/
          cp readme.txt build/kontentainment-lists/ 2>/dev/null || :
          # Clean build from dev assets
          rm -rf build/kontentainment-lists/**/*.zip
          rm -rf build/kontentainment-lists/.git*
          rm -rf build/kontentainment-lists/.agent*
          rm -rf build/kontentainment-lists/tests
          
      - name: Create ZIP
        run: |
          cd build
          zip -r kontentainment-lists.zip kontentainment-lists/
          
      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: build/kontentainment-lists.zip
          draft: false
          prerelease: false
          generate_release_notes: true
