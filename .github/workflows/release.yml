name: Production Release Build

on:
  push:
    tags:
      - 'v*'

jobs:
  build_release:
    name: Build and Upload Release Archive
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Preparation
        run: |
          mkdir -p build/charts
          cp -r admin build/charts/
          cp -r inc build/charts/
          cp -r public build/charts/
          cp -r languages build/charts/ 2>/dev/null || :
          cp charts.php build/charts/
          cp readme.txt build/charts/ 2>/dev/null || :
          # Clean build from dev assets
          rm -rf build/charts/**/*.zip
          rm -rf build/charts/.git*
          rm -rf build/charts/.agent*
          rm -rf build/charts/tests
          
      - name: Create ZIP
        run: |
          cd build
          zip -r charts.zip charts/
          
      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: build/charts.zip
          draft: false
          prerelease: false
          generate_release_notes: true
